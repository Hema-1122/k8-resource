apiVersion: v1
kind : Service
metadata:
  name : roboshop-service
spec :
    selector :
      project: roboshop
      environment: dev
    ports :
      - protocol : TCP
        port : 80
        targetPort : 80