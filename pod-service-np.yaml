apiVersion: v1
kind : Pod
metadata:
  name : roboshop-service
  labels:
    project: roboshop
    environment: dev
    type : np
spec :
    containers :
     - name : service-demo-container
       image : nginx:latest