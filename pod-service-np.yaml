apiVersion: v1
kind : Service
metadata:
  name : roboshop-service
  labels:
    project: roboshop
    environment: dev
    type : np
spec :
    container :
     - name : service-demo-container
       image : nginx:latest